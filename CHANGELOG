
0.12.0
  - Allow actions to call other actions
  - rename getState() to $state()
  - rename subscribe() to $subscribe()
  
0.11.0
  - rename restated to litestate

1.1.0 
  - Remove chainability, actions can return their own values

1.0.1
  - Fixed immutability issues in deep copy. 

1.0.0
  - change name from restated-lib to restatedjs

  - change constructor, to now have one object
    reStated({
      state: {
        ...
      },
      actions()...
    })

  - retrieve the state directy:
    const store = reStated({
      state: {
        myKey: 'Hi'
      }
    })
    console.log(store.myKey)
  